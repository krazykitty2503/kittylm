# KittyLM Data Sources

No dataset has been built yet.

This file is a generated, validated artifact. Once the dataset tooling exists (plan steps 3–5),
it is regenerated from the committed dataset manifests: one row per source with its license,
declared version, files and bytes kept/dropped, and drop reasons. Generation is deterministic
(sorted rows, no timestamps, no absolute paths), and CI regenerates the file and fails if the
committed copy differs.

Until the first manifest is committed, there is nothing to compare, and CI only checks that
this file states that no dataset has been built.
